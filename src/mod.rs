use std::collections::HashMap;
use crate::framework::structures::BaseConfig;
pub fn get_BaseConfigs() -> HashMap<String, base_config> {
    let mut map = HashMap::new();
    map
}
use std::collections::HashMap;
use crate::framework::structures::Attack;
pub fn get_Attacks() -> HashMap<String, attacks> {
    let mut map = HashMap::new();
    map
}
use std::collections::HashMap;
use crate::framework::structures::Mitigation;
pub fn get_Mitigations() -> HashMap<String, mitigations> {
    let mut map = HashMap::new();
    map
}
